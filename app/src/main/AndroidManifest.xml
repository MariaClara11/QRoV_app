<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.application.qrov">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.SobreActivity"
            android:theme="@style/AppTheme.ActionBar" />
        <activity
            android:name=".activities.TermosActivity"
            android:theme="@style/AppTheme.ActionBar" />
        <activity
            android:name=".activities.AjudaActivity"
            android:theme="@style/AppTheme.ActionBar" />
        <activity android:name=".activities.EstoqueActivity" />
        <activity android:name=".activities.ListaProdutosActivity" />
        <activity android:name=".activities.ProdutoActivity" />
        <activity android:name=".activities.MainActivity" />
        <activity
            android:name=".activities.Captura"
            android:screenOrientation="portrait"
            android:stateNotNeeded="true"
            android:theme="@style/zxing_CaptureTheme"
            android:windowSoftInputMode="stateAlwaysHidden" />
        <activity android:name=".activities.OpenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>